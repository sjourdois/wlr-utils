#? Config file for btop v. 1.4.6
# btop config for the demo desktop. Graph boxes only: the process list would put
# the real machine's command lines in a published capture.
color_theme = "Default"
theme_background = True
truecolor = True
shown_boxes = "cpu mem"
update_ms = 250
disks_filter = "/"
proc_filter_kernel = True
cpu_bottom = False
show_uptime = True
check_temp = True
show_battery = False
