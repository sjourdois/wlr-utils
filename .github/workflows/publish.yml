name: Publish

on:
  push:
    tags: ['v*']

jobs:
  crates-io:
    runs-on: ubuntu-latest
    env:
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install system dependencies
        if: env.CARGO_REGISTRY_TOKEN != ''
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libwayland-dev libxkbcommon-dev libfontconfig-1-dev \
            libegl1-mesa-dev libgl1-mesa-dev
      - name: Publish to crates.io
        if: env.CARGO_REGISTRY_TOKEN != ''
        run: |
          if cargo publish 2>publish-err.log; then
            echo "published"
          elif grep -qi 'already' publish-err.log; then
            echo "this version is already on crates.io — skipping"
            cat publish-err.log
          else
            cat publish-err.log >&2
            exit 1
          fi
